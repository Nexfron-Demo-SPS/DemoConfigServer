spring:
  application:
    name: demoConfigServer
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/Users/pt071/config-repo


server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"
